import { PlatoFilterPipe } from './plato-filter.pipe';

describe('PlatoFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PlatoFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
